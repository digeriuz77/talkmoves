// Placeholder asset URLs - using CSS gradients instead of images
export type AssetUrls = {
  classroom_normal: string;
  classroom_beasts: string;
  classroom_transformers: string;
};

// Default placeholder URLs (we'll use CSS gradients instead)
export const DEFAULT_ASSETS: AssetUrls = {
  classroom_normal: '',
  classroom_beasts: '',
  classroom_transformers: '',
};
